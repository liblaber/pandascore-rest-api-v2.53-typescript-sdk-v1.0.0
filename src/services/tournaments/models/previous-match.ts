// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';

/**
 * The shape of the model inside the application code - what the users use
 */
export const previousMatch = z.lazy(() => {
  return z.object({
    matchId: z.number().gte(1),
    type: z.string(),
  });
});

/**
 *
 * @typedef  {PreviousMatch} previousMatch
 * @property {number}
 * @property {PreviousMatchType}
 */
export type PreviousMatch = z.infer<typeof previousMatch>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const previousMatchResponse = z.lazy(() => {
  return z
    .object({
      match_id: z.number().gte(1),
      type: z.string(),
    })
    .transform((data) => ({
      matchId: data['match_id'],
      type: data['type'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const previousMatchRequest = z.lazy(() => {
  return z.object({ matchId: z.number().nullish(), type: z.string().nullish() }).transform((data) => ({
    match_id: data['matchId'],
    type: data['type'],
  }));
});
