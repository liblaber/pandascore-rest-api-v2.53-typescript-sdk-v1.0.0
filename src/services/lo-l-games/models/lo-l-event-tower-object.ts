// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';

/**
 * The shape of the model inside the application code - what the users use
 */
export const loLEventTowerObject = z.lazy(() => {
  return z.object({
    name: z.string(),
    side: z.string(),
  });
});

/**
 *
 * @typedef  {LoLEventTowerObject} loLEventTowerObject
 * @property {LoLEventTowerValue}
 * @property {LoLTeamColor}
 */
export type LoLEventTowerObject = z.infer<typeof loLEventTowerObject>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const loLEventTowerObjectResponse = z.lazy(() => {
  return z
    .object({
      name: z.string(),
      side: z.string(),
    })
    .transform((data) => ({
      name: data['name'],
      side: data['side'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const loLEventTowerObjectRequest = z.lazy(() => {
  return z.object({ name: z.string().nullish(), side: z.string().nullish() }).transform((data) => ({
    name: data['name'],
    side: data['side'],
  }));
});
