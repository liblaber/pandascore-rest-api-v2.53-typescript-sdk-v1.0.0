// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';
import { id, idRequest, idResponse } from './id';

/**
 * The shape of the model inside the application code - what the users use
 */
export const gameWinner = z.lazy(() => {
  return z.object({
    id: id,
    type: z.string().nullable(),
  });
});

/**
 *
 * @typedef  {GameWinner} gameWinner
 * @property {Id}
 * @property {GameWinnerType2}
 */
export type GameWinner = z.infer<typeof gameWinner>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const gameWinnerResponse = z.lazy(() => {
  return z
    .object({
      id: idResponse,
      type: z.string().nullable(),
    })
    .transform((data) => ({
      id: data['id'],
      type: data['type'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const gameWinnerRequest = z.lazy(() => {
  return z.object({ id: idRequest.nullish(), type: z.string().nullish() }).transform((data) => ({
    id: data['id'],
    type: data['type'],
  }));
});
