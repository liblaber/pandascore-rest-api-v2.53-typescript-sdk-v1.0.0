// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';

/**
 * The shape of the model inside the application code - what the users use
 */
export const searchOverValorantAbilities = z.lazy(() => {
  return z.object({
    abilityType: z.string().optional(),
    name: z.string().optional(),
  });
});

/**
 *
 * @typedef  {SearchOverValorantAbilities} searchOverValorantAbilities
 * @property {ValorantAbilityType} - Ability type
 * @property {string} - Name of the ability
 */
export type SearchOverValorantAbilities = z.infer<typeof searchOverValorantAbilities>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const searchOverValorantAbilitiesResponse = z.lazy(() => {
  return z
    .object({
      ability_type: z.string().optional(),
      name: z.string().optional(),
    })
    .transform((data) => ({
      abilityType: data['ability_type'],
      name: data['name'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const searchOverValorantAbilitiesRequest = z.lazy(() => {
  return z.object({ abilityType: z.string().nullish(), name: z.string().nullish() }).transform((data) => ({
    ability_type: data['abilityType'],
    name: data['name'],
  }));
});
